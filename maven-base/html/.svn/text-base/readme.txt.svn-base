The about-pre.html file gets filtered, then copied
into any wars by the pregoal defined in maven-base.
I'm not entirely happy with this approach - it's a bit
fragile - but better than doing this change for every war.
Better solutions gratefully accepted